# ui-kit 模块（renderer 侧）

负责会话：ui-kit（第 1 批）。无契约依赖。
职责：shadcn 原语 + 设计 token（配色/圆角/间距/字体）。
shadcn 组件入仓处为 `@/components/ui`；设计 token 在 `src/renderer/src/index.css`。
本会话（脚手架）已配好 shadcn/ui 与 Tailwind v4，未做任何业务组件。
